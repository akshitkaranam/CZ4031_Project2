def indexscan(index_name):
    annotation = "The table is read using index scan. " \
                 + "This is because there is an index:" + index_name + "created on the table."

    return annotation
